import HomeButton from '@/components/elements/HomeButton';

const Lobby = () => {
  return (
    <div>
      {/* ホームボタンコンポーネント */}
      <div>
        <HomeButton />

        
      </div>
    </div>
  );
};

export default Lobby;
